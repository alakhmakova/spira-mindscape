package com.spiramindscape.android.ui.theme

import androidx.compose.ui.graphics.Color

// Spira brand tokens mirrored from the web app (see specs/tech-stack.md "Styling strategy").
// Keep these in sync with the web CSS variables so the two surfaces stay coherent.
val SpiraOrange = Color(0xFFEA580C) // primary
val SpiraOrangeDark = Color(0xFFF97316)
val SpiraSoft = Color(0xFFFFF1E9) // primary-soft
val SpiraInk = Color(0xFF1C1917) // foreground
val SpiraSurfaceDark = Color(0xFF1C1917)
val SpiraOnDark = Color(0xFFF5F5F4)
