package com.spiramindscape.android.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type scale for now. When we mirror the web type scale more closely
// (Styling strategy in the plan), adjust the individual TextStyles here.
val SpiraTypography = Typography()
