# References for Goal Card Layout Refinement

## Similar Implementations

### Current GoalCard
- **Location:** `src/components/spira/GoalCard.tsx`
- **Relevance:** This is the primary file being updated. It contains the exact data props (`Goal` type) and interactive elements (`ConfirmDialog`, `DeadlinePopover`, `ProgressBar`, `ConfidencePill`) that must be preserved during the redesign.

### Current Main Page
- **Location:** `src/routes/index.tsx`
- **Relevance:** The main container wrapper grid and background colors will be slightly adjusted here to match the target gray background.
