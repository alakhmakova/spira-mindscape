# CI black-box (e2e) profile. The Python suite fires hundreds of requests from
# one IP/user in seconds; rate limiting would throttle it (429s). Disable it
# here — the limiter itself is unit-tested separately (RateLimitFilterTest).
spira.ratelimit.enabled=false
