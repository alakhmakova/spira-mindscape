# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.npm-cache/

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
src/routeTree.gen.ts
.nitro
*.local

# Local environment and tool state
.env
.env.*
!.env.example
!.env.*.example
.claude/

# Backend build/cache output
backend/target/
backend/.gradle/
backend/gradle/
backend/.mvn/wrapper/apache-maven-*/
backend/.mvn/wrapper/*.zip

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Firebase / Android — google-services.json holds an API key; keep it out of the
# public repo. It lives locally at android/app/ and is fetched per-machine via
# `firebase apps:sdkconfig` (see docs/mobile-setup-guide.md, Part B).
android/app/google-services.json
google-services.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
